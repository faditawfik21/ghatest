name: Trigger auto deployment for ft-aca-manual-test

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ 37053800 ]
    paths:
    - '**'
    - '.github/workflows/ft-aca-manual-test-AutoDeployTrigger-72f1fc48-f739-4f24-9a20-d24c52c136da.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.FTACAMANUALTEST_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.FTACAMANUALTEST_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.FTACAMANUALTEST_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: ftmainregistry.azurecr.io
          registryUsername: ${{ secrets.FTACAMANUALTEST_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.FTACAMANUALTEST_REGISTRY_PASSWORD }}
          containerAppName: ft-aca-manual-test
          resourceGroup: faditawfik-rg
          imageToBuild: ftmainregistry.azurecr.io/ft-aca-manual-test:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


